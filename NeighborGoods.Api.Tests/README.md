# NeighborGoods.Api.Tests

Integration tests spin up **SQL Server inside Docker** via **Testcontainers** (`SqlServerContainerFixture`). Ensure Docker is running before `dotnet test`.

See the root [README.md](../README.md) section **4.4 Backend integration tests** for details.
