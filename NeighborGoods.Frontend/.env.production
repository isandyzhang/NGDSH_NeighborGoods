# 由 Vite 在 `npm run build`（production）時自動載入並內嵌至 bundle。
# 僅含公開網址，不含密鑰。若要以 CI 覆寫，請在 workflow 設相同變數名（優先於本檔）。
VITE_API_BASE_URL=https://api.neighborgoodstw.com
VITE_SIGNALR_BASE_URL=https://api.neighborgoodstw.com
# LIFF shareTargetPicker 需要設定此值；未設定時會退回一般分享（不會送 Flex）
# VITE_LINE_LIFF_ID=2008745853-YourLiffSuffix
