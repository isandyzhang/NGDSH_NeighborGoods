VITE_API_BASE_URL=http://localhost:5065
VITE_SIGNALR_BASE_URL=http://localhost:5065

# LINE Login channel 建立的 LIFF ID（與後端 LineMessagingApi:LiffId 一致；綁定官方通知用）
# VITE_LINE_LIFF_ID=2008745853-YourLiffSuffix

# Production：`npm run build` 會讀取已提交的 `.env.production`（公開 API 網址），
# 或由 CI／shell 以同名環境變數覆寫。
